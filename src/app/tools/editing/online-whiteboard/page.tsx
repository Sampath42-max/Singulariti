import React from 'react';
import WhiteboardClientWrapper from '@/components/tools/whiteboard/WhiteboardClientWrapper';

export const metadata = {
  title: "Online Whiteboard - Singularity",
  description: "Draw, write, sketch, add shapes, and export your whiteboard directly in your browser using Singularity."
};

export default function OnlineWhiteboardPage() {
  return <WhiteboardClientWrapper />;
}
